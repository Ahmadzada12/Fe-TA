{
  "builds": [
    {
      "src": "vite.config.js", // atau "vue.config.js" untuk Vue CLI
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [
    {
      "src": "/[^.]+",
      "dest": "/index.html"
    }
  ]
}
